# Dependencies for the Streamlit dashboard (feature 4, ADR-007).
# Imagen ligera ~240 MB sin TF/PySpark/Java/Pillow.
# streamlit>=1.37 es REQUISITO para usar `st.fragment` estable
# (en 1.36 todavia era `st.experimental_fragment`).
streamlit==1.39.0
httpx==0.27.0
plotly==5.22.0
pandas==2.2.2
# Tests (instalados en la misma imagen para correr la suite del dashboard)
pytest==8.2.0
