pyspark==3.5.1
pymongo==4.7.2
minio==7.2.7
python-dotenv==1.0.1
faker==25.0.0
watchdog==4.0.0
fastapi==0.111.0
uvicorn[standard]==0.30.0
httpx==0.27.0
sqlalchemy==2.0.30
pytest==8.2.0
# ML stack for radiography classification (Feature 2, ADR-005/006).
# Using `tensorflow` (not `tensorflow-cpu`) because the -cpu wheel is only
# published for linux x86_64; in arm64 (Apple Silicon, etc) only the plain
# `tensorflow` package has a CPU-only wheel.
tensorflow==2.16.1
scikit-learn==1.5.0
matplotlib==3.9.0
pillow==10.3.0
